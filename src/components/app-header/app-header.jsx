import React from "react";

const AppHeader = () => (
    <header className="header navbar navbar-dark bg-primary">
        <div className="container">
            <a className="navbar-brand" href="/">Countries of the World</a>
        </div>
    </header>
);

export default AppHeader;
